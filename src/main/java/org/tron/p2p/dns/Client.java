package org.tron.p2p.dns;

import org.tron.p2p.protos.Connect;

public class Client {

  Connect.DnsRoot queryDns(Connect.DnsDomain dnsDomain) {
    try {
      byte[] value = null;
      //1. nds interface get values
      Sna
      Connect.DnsRoot dnsRoot = Connect.DnsRoot.parseFrom(value);
      //2. valid sign
      return dnsRoot;
    } catch (Exception e) {

    }

  }
}
